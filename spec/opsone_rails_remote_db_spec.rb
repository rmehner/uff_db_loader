RSpec.describe OpsoneRailsRemoteDb do
  it "has a version number" do
    expect(OpsoneRailsRemoteDb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
